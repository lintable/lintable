# see http://nvie.com/posts/better-package-management/ for more details
# put only the libraries we explicitly require
# any dependencies will be determined by pip-compile
#
# 1) Get pip-compile by running: pip install pip-tools
# 2) Run pip-compile with no arguments: pip-compile
# 3) Run pip install off the generated requirements.txt: pip install -r requirements.txt
#
# You can use pip-sync to remove any unneeded packages after cleaning up the requirements.in
Flask==0.10.1
Gunicorn==19.4.1
Celery==3.1.19
Redis==2.10.5
GitPython==1.0.1
peewee==2.8.0
psycopg2==2.6.1
Requests==2.9.1
simple-crypt==4.1.7
PyGithub==1.26.0
click==6.2
rstr==2.2.3
flask-login==0.3.2
cryptography==1.2.2
markdown==2.6.5
